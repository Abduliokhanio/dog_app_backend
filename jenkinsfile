pipeline {
  agent any

  triggers {
      githubPush()
  }

  environment {
        BRANCH = 'master'
    }

  stages {
    stage('seeing if connection to github worked') {
            steps {
                echo 'conneted to github baby'
            }
        }
  }
}